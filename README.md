# SmartLibrary Setup

# Dependencies
This is an application that helps to manage the libray system of Limkokwing University Of Ceative Technology Sierra leone
as it helps the university to keep records of the library operations digitaly.

# Installaion Process
Python 3.10+

pip install psycopg2-binary PyQt5 python-dotenv

# Steps

Sign up at neon.tech, create project, copy DB URL.
Create .env: NEON_DB_URL=postgresql://user:pass@host/db
Run SQL script (in doc) on Neon.
python main.py

Sample Data: Admin (admin/admin123), Alice (alice/alice123).
